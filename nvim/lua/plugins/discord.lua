-- doesn't show when im in the terminal
-- side note: why is the terminal called C?
return {
  "andweeb/presence.nvim",
  config = true,
  opts = {
    -- log_level = "debug",
    enable_line_number = false,
    neovim_image_text = "Text Editor in the terminal",
  },
}
