-- need lsp (and lua doesn't even work then?)
-- for logging, turn it on, then
-- check logs: ':messages'

return {
  "soulis-1256/eagle.nvim",
  opts = {
    mouse_mode = true,
    keyboard_mode = true,
    -- logging = true,
  },
}
